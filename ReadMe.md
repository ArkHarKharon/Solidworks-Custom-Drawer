# Drawer.cs - Библиотека для работы с SolidWorks API

Библиотека упрощает процесс создания моделей в SolidWorks, собирая наиболее используемые методы API в одном месте.

## 📋 Содержание

- [Установка и настройка](#-установка-и-настройка)
- [Быстрый старт](#-быстрый-старт)
- [Инициализация проекта](#-инициализация-проекта)
- [Работа с эскизами](#️-работа-с-эскизами)
- [Работа с деталями](#-работа-с-деталями)
- [Работа со сборками](#-работа-со-сборками)
- [Вспомогательные классы](#️-вспомогательные-классы)
- [Перечисления (Enums)](#-перечисления-enums)
- [Советы и рекомендации](#-советы-и-рекомендации)
- [Важные замечания](#️-важные-замечания)
- [Примеры использования](#-примеры-использования)

---

## 🔧 Установка и настройка

### Требования
- SolidWorks (установленный на компьютере)
- .NET Framework
- Ссылки на SolidWorks Interop библиотеки:
  - `SolidWorks.Interop.sldworks`
  - `SolidWorks.Interop.swconst`

### Рекомендуемые настройки SolidWorks
Для корректной работы библиотеки рекомендуется отключить привязки:
1. Откройте **Параметры** (иконка шестеренки)
2. Перейдите в **Эскиз → Взаимосвязи/привязки**
3. Отключите **"Разрешить привязки"**

### Единицы измерения
Библиотека работает с **миллиметрами**. Все размеры автоматически конвертируются в метры для API.

---

## 🚀 Быстрый старт

```csharp
using Temp;

// Создание экземпляра библиотеки
SWDrawer drawer = new SWDrawer();

// Инициализация SolidWorks
drawer.init();

// Создание новой детали
drawer.newProject(DocumentType.PART);

// Быстрое создание куба 1х1х1 метр (для тестирования)
drawer.fastCube();
```

---

## 🎯 Инициализация проекта

### Подключение к SolidWorks

```csharp
SWDrawer drawer = new SWDrawer();

// Подключается к открытому SolidWorks или запускает новый
bool success = drawer.init();
```

### Создание нового проекта

```csharp
// Создание детали
drawer.newProject(DocumentType.PART);

// Создание сборки
drawer.newProject(DocumentType.ASSEMBLY);

// Создание чертежа (можно указать номер шаблона)
drawer.newProject(DocumentType.DRAWING, 10);
```

### Подключение к открытой детали

```csharp
// Если деталь уже открыта в SolidWorks
bool connected = drawer.connectToOpenedPart();
```

### Подключение к открытой сборке

```csharp
bool connected = drawer.connectToOpenedAssembly();
```

---

## ✏️ Работа с эскизами

### Выбор плоскости

```csharp
// Выбор стандартных плоскостей
drawer.selectDefaultPlane(DefaultPlaneName.TOP);     // Сверху
drawer.selectDefaultPlane(DefaultPlaneName.FRONT);   // Спереди
drawer.selectDefaultPlane(DefaultPlaneName.RIGHT);   // Справа
```

### Открытие/закрытие эскиза

```csharp
// Открыть эскиз на выбранной плоскости
drawer.insertSketch(true);

// Закрыть эскиз
drawer.insertSketch(false);
```

### Создание геометрии

#### Линии

```csharp
// Линия по координатам начальной и конечной точки
SketchSegment line = drawer.createLineByCoords(
    0, 0, 0,      // Начало (x, y, z)
    0.1, 0.1, 0   // Конец (x, y, z)
);
```

#### Окружности

```csharp
// Окружность по центру и радиусу
SketchSegment circle1 = drawer.createCircleByRadius(
    0, 0, 0,    // Центр (x, y, z)
    0.05        // Радиус
);

// Окружность по центру и точке на окружности
SketchSegment circle2 = drawer.createCircleByPoint(
    0, 0, 0,        // Центр
    0.05, 0, 0      // Точка на окружности
);
```

#### Прямоугольники

```csharp
// Прямоугольник по двум противоположным углам
drawer.createRectangle(
    0, 0, 0,        // Первый угол
    0.1, 0.1, 0     // Противоположный угол
);

// Прямоугольник по центру и углу
drawer.createCenterRectangle(
    0, 0, 0,        // Центр
    0.05, 0.05, 0   // Угол
);
```

### Обрезка элементов

```csharp
// Сохраняем элемент для последующей обрезки
SketchSegment circle = drawer.createCircleByRadius(0, 0, 0, 0.1);

// Обрезаем в указанной точке
drawer.trim(circle, 0.05, 0.05, 0);
```

### Пример: Создание простого эскиза

```csharp
drawer.selectDefaultPlane(DefaultPlaneName.TOP);
drawer.insertSketch(true);

// Создаём прямоугольник
drawer.createCenterRectangle(0, 0, 0, 0.05, 0.05, 0);

// Создаём окружность в центре
SketchSegment circle = drawer.createCircleByRadius(0, 0, 0, 0.02);

drawer.insertSketch(false); // Закрываем эскиз
```

---

## 🔨 Работа с деталями

### Вытягивание

```csharp
drawer.selectDefaultPlane(DefaultPlaneName.TOP);
drawer.insertSketch(true);
drawer.createCenterRectangle(0, 0, 0, 0.05, 0.05, 0);

// Вытягивание на 0.1 метра
drawer.extrude(0.1);

// Вытягивание в противоположном направлении
drawer.extrude(0.1, changeDirection: true);
```

### Вырезание отверстий

```csharp
drawer.selectDefaultPlane(DefaultPlaneName.TOP);
drawer.insertSketch(true);
drawer.createCircleByRadius(0, 0, 0, 0.01);

// Вырезать насквозь
drawer.cutHole(HoleType.CUT_THROUGH);

// Вырезать на заданную глубину
drawer.cutHole(HoleType.DISTANCE, depth: 0.05);

// Вырезать с отступом от поверхности
drawer.cutHole(HoleType.OFFSET, depth: 0.03);
```

### Работа с телами и гранями

```csharp
// Получить все тела детали
Body2[] bodies = drawer.getAllBodies();

// Получить все грани тела
Face2[] faces = drawer.getAllFaces(bodies[0]);

// Выбрать грань по индексу
drawer.SelectFaceByIndex(bodies[0], 0);

// Просмотр всех граней с их индексами (отладка)
drawer.viewBodyFaces(bodies[0]);
```

### Работа с рёбрами

```csharp
Body2[] bodies = drawer.getAllBodies();
Face2 face = drawer.getAllFaces(bodies[0])[0];

// Получить все рёбра грани
Edge[] edges = drawer.getAllEdges(face);

// Выбрать ребро по индексу
drawer.SelectEdgeByIndex(face, 0);

// Просмотр всех рёбер грани (отладка)
drawer.viewFaceEdges(0);
```

### Скругления

```csharp
// Скругление рёбер 0, 1, 2 грани 5 с радиусом 0.005м
drawer.createFillets(
    faceNum: 5,
    radius: 0.005,
    edgeNums: 0, 1, 2
);
```

### Фаски

```csharp
// Фаска на рёбрах с двумя расстояниями
drawer.createChamfers(
    faceNum: 5,
    distance1: 0.002,
    distance2: 0.003,
    edgeNums: 0, 1
);
```

### Удаление элементов

```csharp
// Удалить последние 3 элемента из дерева построений
drawer.deleteLastFeatures(3);
```

### Полный пример создания детали

```csharp
SWDrawer drawer = new SWDrawer();
drawer.init();
drawer.newProject(DocumentType.PART);

// Создаём основание
drawer.selectDefaultPlane(DefaultPlaneName.TOP);
drawer.insertSketch(true);
drawer.createCenterRectangle(0, 0, 0, 0.05, 0.05, 0);
drawer.extrude(0.02);

// Создаём отверстие
Body2[] bodies = drawer.getAllBodies();
drawer.SelectFaceByIndex(bodies[0], 0); // Верхняя грань
drawer.insertSketch(true);
drawer.createCircleByRadius(0, 0, 0, 0.01);
drawer.cutHole(HoleType.CUT_THROUGH);

// Скругляем рёбра
drawer.createFillets(0, 0.002, 0, 1, 2, 3);
```

---

## 🔩 Работа со сборками

### Импорт деталей

```csharp
drawer.newProject(DocumentType.ASSEMBLY);

// Импорт одной детали
drawer.importPart(
    @"C:\Projects\Parts\bolt.SLDPRT",
    x: 0,
    y: 0,
    z: 0
);
```

### Импорт из папки

```csharp
// Импортировать все детали из папки
// Детали располагаются сеткой 5x5
drawer.importPartsFromFolder(
    @"C:\Projects\Parts\",
    offset: 0.1,      // Расстояние между деталями
    rawLength: 5      // Количество деталей в ряду
);
```

### Получение тел сборки

```csharp
List<Body2> bodies = drawer.GetAllBodiesFromAssembly();
```

---

## 🛠️ Вспомогательные классы

### HolesArrayCutter - массив отверстий

Класс для автоматического создания массива отверстий в заданной области.

#### Использование

```csharp
SWDrawer drawer = new SWDrawer();
drawer.init();
drawer.connectToOpenedPart();

HolesArrayCutter cutter = new HolesArrayCutter(drawer);

// Выделите 2 вершины в модели, которые образуют область
// Затем вызовите метод:

cutter.cutHoles(
    rowsNum: 5,          // Количество рядов
    columsNum: 5,        // Количество столбцов
    verticiesNum: 6,     // Вершин в многоугольнике (6 = шестиугольник)
    angle: 0.0,          // Угол поворота (градусы)
    offset: 0.005,       // Отступ от граней области
    reverse: false       // Обратное направление
);
```

#### Параметры отверстий

- `rowsNum` - количество отверстий по вертикали
- `columsNum` - количество отверстий по горизонтали
- `verticiesNum` - количество сторон многоугольника (3 = треугольник, 4 = квадрат, 6 = шестиугольник и т.д.)
- `angle` - угол поворота многоугольника в градусах
- `offset` - отступ от верхней и нижней граней области (если 0, то вырезает насквозь)

---

## 📚 Перечисления (Enums)

### DocumentType
```csharp
DocumentType.DRAWING    // Чертёж
DocumentType.PART       // Деталь
DocumentType.ASSEMBLY   // Сборка
```

### DefaultPlaneName
```csharp
DefaultPlaneName.TOP    // Сверху
DefaultPlaneName.FRONT  // Спереди
DefaultPlaneName.RIGHT  // Справа
```

### HoleType
```csharp
HoleType.CUT_THROUGH    // Вырезать насквозь
HoleType.DISTANCE       // Вырезать на глубину
HoleType.OFFSET         // Вырезать с отступом от поверхности
```

### ObjectType
```csharp
ObjectType.EDGE         // Ребро
ObjectType.FACE         // Грань
ObjectType.VERTEX       // Вершина
ObjectType.PLANE        // Плоскость
ObjectType.AXIS         // Ось
ObjectType.SKETCH_SEGMENT // Сегмент эскиза
```

---

## 💡 Советы и рекомендации

### Сохранение элементов эскиза

Для последующей работы с элементами (обрезка, модификация) сохраняйте их в переменные:

```csharp
SketchSegment circle = drawer.createCircleByRadius(0, 0, 0, 0.05);
SketchSegment line = drawer.createLineByCoords(0, 0, 0, 0.1, 0, 0);

// Теперь можно работать с этими элементами
drawer.trim(circle, 0.03, 0.03, 0);
```

### Определение индексов граней и рёбер

Используйте отладочные методы для визуального определения индексов:

```csharp
Body2[] bodies = drawer.getAllBodies();

// Показать все грани с их индексами
drawer.viewBodyFaces(bodies[0]);

// Показать все рёбра грани с индексами
drawer.viewFaceEdges(0);
```

### Работа с единицами измерения

Все размеры вводятся в **миллиметрах**, но внутри методов автоматически конвертируются в **метры**:

```csharp
// Если вы хотите куб 100x100x100 мм:
double size = 100 / 1000.0; // = 0.1 метра
drawer.createCenterRectangle(0, 0, 0, size/2, size/2, 0);
drawer.extrude(size);
```

---

## ⚠️ Важные замечания

1. **Не забывайте закрывать эскизы** после работы с ними (`insertSketch(false)`)
2. **Выходите из эскиза перед вытягиванием** - методы `extrude()` и `cutHole()` работают только внутри эскиза
3. **Сохраняйте элементы в переменные** если планируете работать с ними позже
4. **Используйте отладочные методы** (`viewBodyFaces`, `viewFaceEdges`) для определения индексов
5. **Проверяйте направление вытягивания** - если не в ту сторону, используйте параметр `changeDirection: true`

---

## 📝 Примеры использования

### Создание простой детали с отверстием

```csharp
SWDrawer drawer = new SWDrawer();
drawer.init();
drawer.newProject(DocumentType.PART);

// Основание
drawer.selectDefaultPlane(DefaultPlaneName.TOP);
drawer.insertSketch(true);
drawer.createCenterRectangle(0, 0, 0, 0.05, 0.05, 0);
drawer.extrude(0.02);

// Отверстие
Body2[] bodies = drawer.getAllBodies();
drawer.SelectFaceByIndex(bodies[0], 0);
drawer.insertSketch(true);
drawer.createCircleByRadius(0, 0, 0, 0.01);
drawer.cutHole(HoleType.CUT_THROUGH, changeDirection: true);
```

### Создание сборки из папки деталей

```csharp
SWDrawer drawer = new SWDrawer();
drawer.init();
drawer.newProject(DocumentType.ASSEMBLY);

drawer.importPartsFromFolder(
    @"C:\MyProject\Parts\",
    offset: 0.15,
    rawLength: 4
);
```

---

## 🤝 Авторы

Библиотека создана для упрощения работы с SolidWorks API и помощи в изучении возможностей программы.

Основной функционал библиотеки заложил [@ArkHarKharon](https://github.com/ArkHarKharon), также в разработке приложил свою руку [@KingArtur1000](https://github.com/KingArtur1000).

## 📄 Лицензия

Свободное использование в учебных и коммерческих проектах.